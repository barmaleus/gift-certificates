apply plugin: 'java'

bootJar {
  baseName = 'gift-certificates'
  version = '1.0-SNAPSHOT'
}

dependencies {
  compile group: 'org.springframework.boot', name: 'spring-boot-starter-web'
  compile project(':service')
}
